  Đọc tasklist tại `tools/alpha-crm/docs/specs/mobile-web-completion-tasklist.md` (gap analysis đã làm xong, không cần phân tích lại).  Triển khai Sprint 3 

  xây CrmEventHub, endpoint SSE /api/crm/events/subscribe, broadcast sự kiện, nâng cấp heartbeat
  + offline detector, và long-poll /agent/commands/next trong `alpha-studio-backend`.

  Yêu cầu:

  - Tuân thủ DoD ghi trong từng task; giữ backward-compatible với agent/client cũ.
  - KHÔNG đổi hành vi Live Chat desktop (local bridge) — xem cảnh báo rủi ro FE-1 và QA-2.
  - Với AG-3/BE-6: giữ chính sách privacy nhóm managed metadata-only như inbound hiện tại.
  - Xong mỗi sprint: chạy verify nhỏ nhất liên quan, báo cáo lại, chờ tôi xác nhận rồi mới sang sprint kế.
  - Kết thúc: cập nhật PROJECT_SUMMARY.md của các project bị ảnh hưởng theo DOC-1.

  Nếu muốn chạy từng phần riêng lẻ, chỉ cần thay dòng thứ hai bằng phạm vi hẹp hơn, ví dụ: "Chỉ triển khai Sprint 2 (AG-1, AG-2, AG-3)   
  trong tools/alpha-crm/integration/zalo-bot-service" hoặc "Chỉ triển khai Sprint 3 (FE-1 → FE-5) phía Flutter".